name: Update GH Pages
on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: "Update GH Pages"
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: bash update-gh-pages.sh
